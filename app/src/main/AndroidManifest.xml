<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.dawaa">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignupActivity" />
        <activity android:name=".SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginActivity" />
        <activity android:name=".DocActivity" />
        <activity android:name=".PharmActivity" />
        <activity android:name=".MedicineActivity" />
        <activity android:name=".MedicinesListActivity" />
        <activity android:name=".AddMedicineActivity" />
        <activity android:name=".Alarm2Activity" />
        <activity android:name=".MeasurementsActivity" />
        <activity android:name=".MedInfoActivity" />
        <activity android:name=".Calendar2Activity" />
        <activity android:name=".DocListActivity" />
        <activity android:name=".PharmListActivity" />
        <activity android:name=".AddDocActivity" />
        <activity android:name=".AddPharmActivity" />

        <receiver
            android:name=".AlertReceiver"
            android:enabled="true"
            android:exported="false" />
        <!--
             <receiver
            android:name=".AlarmBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />
        -->

    </application>

</manifest>